# ComfyUI Workflows

Store API-format workflow JSON files here. The production graph should load a model, inject one positive prompt and one negative prompt, apply approved character/reference conditioning, sample, decode and save exactly ONE image per queue job.

Official logo, typography, social icons and footer must not be generated by diffusion. They are deterministic compositor layers.
